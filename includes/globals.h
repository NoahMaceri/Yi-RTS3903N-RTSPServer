#ifndef GLOBALS_H
#define GLOBALS_H

#define VIDEO_SINK "/tmp/rtsp_video_fifo"
#define AUDIO_SINK "/tmp/rtsp_audio_fifo"

#define MATCH(s, n) strcmp(section, s) == 0 && strcmp(name, n) == 0

#endif //GLOBALS_H